/** @deprecated Use AppThemeProvider — kept for marketing layout imports. */
export { AppThemeProvider as MarketingThemeProvider, useAppTheme as useMarketingTheme } from "../../context/AppThemeProvider";
